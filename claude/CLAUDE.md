IMPORTANT: If I write in finnish, always reply in english and write all documentation in english. Never reply to me or write anything in Finnish!

EXCEPTION: Game content (oneshots, adventures, session notes, NPC dialogue) for the Nimble TTRPG vault CAN be written in Finnish when explicitly requested. Documentation, code comments, commit messages, and conversational replies always stay in English.

IMPORTANT: Do NOT automatically run background TypeScript compilation checks, type checking (tsc), linting (eslint), or similar validation commands unless explicitly requested by the user. Multiple Claude instances may be running on the same machine, and automatic background checks cause memory exhaustion. Only run these commands when the user specifically asks for type checking, linting, or build validation.
