theme = dracula

keybind = global:alt+tab=toggle_quick_terminal

# Send ESC+p when Option+p is pressed
# keybind = alt+p>send_string:"\x1bp"

shell-integration = zsh
macos-titlebar-style = tabs
# Auto-update Ghostty when a new release is available.
auto-update = download
# gpg pinentry freaks out if $TERM is not set
term = xterm-256color
